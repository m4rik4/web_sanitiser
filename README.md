# web_sanitiser
The project consists in the design, implementation, and evaluation of a web sanitiser tool written in Rust: a defensive utility that inspects web content and neutralises malicious or unwanted material before it can reach the user's system. The tool is delivered as a command-line application backed by a reusable Rust library (crate).
